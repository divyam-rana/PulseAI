# 1) Open Cloud Shell from the GCP Console (top-right “>_” button)

# 2) (Optional) Set your git identity
git config --global user.name "Brendan Wilcox"
git config --global user.email "bjwilcox@bu.edu"

# 3) Clone (public repo or private via HTTPS+token)
git clone https://github.com/divyam-rana/PulseAI.git
cd REPO
